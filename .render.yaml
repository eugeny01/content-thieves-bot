services:
  - type: worker
    name: telegram-content-thieves
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: BOT_TOKEN
        value: your_telegram_bot_token_here
